(ns genetic-algorithm.full-ui.main
  (:gen-class))

; TODO: Add a setting to decide if maybe-judge or force-judge should be used.
; TODO: The latter is necessary if the fitness function isn't pure.

; TODO: Pass in a settings atom to the main procedure so settings can be changed on the fly?

(defn -main []
  (println "Nothing!"))